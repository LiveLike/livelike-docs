---
title: Get Card Metadata
excerpt: >-
  Returns Card metadata without running the query. Check `fully_parameterized` —
  if `false`, look for `required: true` parameters with no `default` and include
  those in your download request.
api:
  file: livelike-analytics-api.json
  operationId: get_card
hidden: false
---